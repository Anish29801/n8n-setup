# n8n Railway Deployment

This repository contains the configuration to deploy the latest version of n8n on Railway via Docker.

See `tasks.md` for full instructions on deployment, configuring environment variables, setting up persistent storage, and integrating Gmail OAuth2.
